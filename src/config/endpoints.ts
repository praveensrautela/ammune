// config/endpoints.ts
import API_BASE_URL from "./api";

const ENDPOINTS = {
    REGISTER_USER: `${API_BASE_URL}/registeruser`,
    LOGIN_USER: `${API_BASE_URL}/loginuser`,
};

export default ENDPOINTS;
